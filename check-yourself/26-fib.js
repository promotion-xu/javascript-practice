// const fib = (n) => {
//   if (n <= 2) return 1;
//   return fib(n - 1) + fib(n - 2);
// };

// console.log(fib(45));

// const fib = (n, prev = 1, next = 1) => {
//   if (n <= 1) return prev;
//   return fib(n - 1, next, next + prev);
// };

// console.log(fib(40));
